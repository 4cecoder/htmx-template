# htmx-template

clone project:
`git clone  https://github.com/4cecoder/htmx-template/`

cd into project then run:


`cd htmx-template`


`go build`

### start your server

`./htmx`

then visit `localhost:8080`
